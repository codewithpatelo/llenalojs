const should = require('chai').should();

const ejemplo = require('../js/ejemplo.js');


describe('TEST1', () => {
  it('If ... is ... it should ...', () => {
	//result = myPackage.myFunction();
    //result.should.equal('');
  });
});

describe('TEST2', () => {
  it('If ... is ... it should ...', () => {
	//result = myPackage.myFunction();
    //result.should.equal('');
  });
});

describe('TEST3', () => {
  it('If ... is ... it should ...', () => {
	//result = myPackage.myFunction();
    //result.should.equal('');
  });
});
